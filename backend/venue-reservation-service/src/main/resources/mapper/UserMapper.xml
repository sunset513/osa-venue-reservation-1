<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tw.edu.ncu.osa.venue_reservation_service.mapper.UserMapper">

    <select id="selectByUserId" resultType="tw.edu.ncu.osa.venue_reservation_service.model.entity.User">
        SELECT user_id, name, email, role, unit_id, login_at, deleted_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="insertPortalUser">
        INSERT INTO users (user_id, name, email, role, login_at, deleted_at)
        VALUES (#{userId}, #{name}, #{email}, #{role}, CURRENT_TIMESTAMP, NULL)
    </insert>

    <update id="updateLoginAt">
        UPDATE users
        SET login_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
    </update>

</mapper>
